[build-system]
requires = [
    "setuptools",
    "wheel",
    ]
build-backend = "setuptools.build_meta"

[project]
name = "workshop-diffusion"
version = "0.1"
dependencies = [
    "matplotlib",
    "numpy",
]

[tool.setuptools]
packages = ["workshop_diffusion"]